# Módulo 2 — Supabase (copia este archivo como .env y completa los valores)
# Usa solo la clave publishable/anon. NUNCA la service_role key en el frontend.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
